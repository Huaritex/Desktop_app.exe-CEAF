# Supabase Configuration
# IMPORTANT: These values should be configured at build time or runtime
# DO NOT commit the actual .env file with real credentials

VITE_SUPABASE_URL=your-supabase-url-here
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# This key should ONLY be used in the main process (electron/main.ts)
# NEVER expose this in the renderer process
SUPABASE_SERVICE_KEY=your-service-key-here
